version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu

hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 300
      runas: ubuntu

  AfterAllowTraffic:
    - location: scripts/after_deploy.sh
      timeout: 60
      runas: ubuntu